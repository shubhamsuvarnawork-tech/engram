[project]
name = "company-brain-backend"
version = "0.1.0"
description = "Company Brain - the Skill Generation Engine and agent runtime."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"
testpaths = ["tests"]
